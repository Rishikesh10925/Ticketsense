import {useEffect,useState} from "react";import {api} from "../api/client";import {Empty,ErrorState,Loading} from "../components/States";
export function Audit(){return <DataPage title="Audit logs" description="Tenant-scoped security and operational events." loader={api.auditLogs}/>}export function Integrations(){return <DataPage title="Integrations" description="Configured enterprise connection providers." loader={api.integrations}/>}export function AIMetrics(){return <DataPage title="AI observability" description="Per-agent calls, latency and confidence from the backend." loader={async()=> (await api.aiMetrics()).agents}/>}function DataPage({title,description,loader}:{title:string;description:string;loader:()=>Promise<Array<Record<string,unknown>>>}){const [items,setItems]=useState<Array<Record<string,unknown>>>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");async function load(){setLoading(true);try{setItems(await loader());setError("")}catch(e){setError(e instanceof Error?e.message:"Request failed")}finally{setLoading(false)}}useEffect(()=>{load()},[]);return <div className="content"><div className="page-title"><div><h1>{title}</h1><p>{description}</p></div><button onClick={load}>Refresh</button></div>{loading?<Loading/>:error?<ErrorState message={error} onRetry={load}/>:items.length?<div className="panel data-list">{items.map((item,index)=><article key={String(item.id||item.name||index)}>{Object.entries(item).filter(([,v])=>typeof v!=="object").map(([k,v])=><div key={k}><span>{k.replaceAll("_"," ")}</span><b>{String(v)}</b></div>)}</article>)}</div>:<Empty/>}</div>}
