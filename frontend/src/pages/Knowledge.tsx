import { useEffect, useState, type FormEvent } from "react";import { api } from "../api/client";import { Empty, ErrorState, Loading } from "../components/States";
export default function Knowledge(){const [q,setQ]=useState("");const [docs,setDocs]=useState<Array<{id:string;title:string;excerpt:string;source?:string;updated_at:string}>>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");async function load(){setLoading(true);setError("");try{setDocs(await api.knowledge(q))}catch(e){setError(e instanceof Error?e.message:"Request failed")}finally{setLoading(false)}}useEffect(()=>{load()},[]);function search(e:FormEvent){e.preventDefault();load()}return <div className="content"><div className="page-title"><div><h1>Knowledge</h1><p>Tenant-scoped articles retrieved from the backend.</p></div></div><form className="filter-bar" onSubmit={search}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search knowledge titles and content"/><button>Search</button></form>{loading?<Loading/>:error?<ErrorState message={error} onRetry={load}/>:docs.length?<div className="knowledge-grid">{docs.map(d=><article className="panel knowledge-card" key={d.id}><span>▤</span><h3>{d.title}</h3><p>{d.excerpt}</p><small>{d.source||"Internal knowledge base"} · Updated {new Date(d.updated_at).toLocaleDateString()}</small></article>)}</div>:<Empty label="No knowledge articles found."/>}</div>}
