import { useState, type FormEvent } from "react";
import { useNavigate } from "react-router-dom";
import { api } from "../api/client";

export default function NewTicket(){const nav=useNavigate();const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=new FormData(e.currentTarget);try{const ticket=await api.createTicket({subject:String(data.get("subject")),description:String(data.get("description")),category:String(data.get("category")||""),product:String(data.get("product")||""),severity:String(data.get("severity")||""),environment:String(data.get("environment")||""),error_message:String(data.get("error_message")||"")});nav(`/tickets/${ticket.id}`,{state:{message:"Ticket created and analyzed successfully."}})}catch(e){setError(e instanceof Error?e.message:"Unable to create ticket")}finally{setBusy(false)}}return <div className="content narrow"><button className="back" onClick={()=>nav(-1)}>← Back</button><form className="panel form-page" onSubmit={submit}><div className="panel-head"><div><h2>Create a support ticket</h2><p>The backend will persist and analyze this ticket.</p></div></div><label>Title<input name="subject" required minLength={4}/></label><label>Description<textarea name="description" required minLength={10} rows={7}/></label><div className="form-row"><label>Category<input name="category" placeholder="Optional — AI can infer"/></label><label>Product<input name="product"/></label></div><div className="form-row"><label>Severity<select name="severity"><option value="">AI managed</option><option>low</option><option>medium</option><option>high</option><option>urgent</option></select></label><label>Environment<input name="environment" placeholder="Production, staging..."/></label></div><label>Error message<input name="error_message"/></label>{error&&<div className="error-box">{error}</div>}<div className="form-actions"><button type="button" onClick={()=>nav(-1)}>Cancel</button><button className="primary" disabled={busy}>{busy?"Analyzing...":"Submit & analyze"}</button></div></form></div>}
